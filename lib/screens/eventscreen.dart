import 'package:flutter/material.dart';


class Eventscreen extends StatelessWidget {
  const Eventscreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("events showe here"),),
    );
  }
}
